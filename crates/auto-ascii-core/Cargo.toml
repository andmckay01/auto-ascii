[package]
name = "auto-ascii-core"
description = "Pure rendering engine for auto-ascii: viewport/letterbox math, separable resampler, glyph ramp palettes, Cell/Grid"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
keywords = ["ascii", "terminal", "rendering", "video", "tui"]
categories = ["rendering", "multimedia::video"]
readme = "../../README.md"

[lints]
workspace = true

# PLAN §8: no deps beyond std (pure, golden-testable).
[dependencies]

# M2 item D: compute_viewport letterbox/aspect property tests (PLAN §6).
[dev-dependencies]
proptest = { workspace = true }
